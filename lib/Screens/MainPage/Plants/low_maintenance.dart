import 'package:flutter/material.dart';

class LowMaintenance extends StatelessWidget {
  const LowMaintenance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
