import 'package:flutter/material.dart';
import 'package:ugaoo/Screens/MainPage/content.dart';

class WinterSeeds extends StatelessWidget {
  const WinterSeeds({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MainContent();
  }
}
