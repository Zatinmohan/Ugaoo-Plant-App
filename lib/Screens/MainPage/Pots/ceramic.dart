import 'package:flutter/material.dart';

class Ceramic extends StatelessWidget {
  const Ceramic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
