import 'package:flutter/material.dart';

class Metallic extends StatelessWidget {
  const Metallic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
