import 'package:flutter/material.dart';

class Concerete extends StatelessWidget {
  const Concerete({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
