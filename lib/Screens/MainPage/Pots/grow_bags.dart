import 'package:flutter/material.dart';

class GrowBags extends StatelessWidget {
  const GrowBags({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
