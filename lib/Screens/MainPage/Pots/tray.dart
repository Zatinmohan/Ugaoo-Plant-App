import 'package:flutter/material.dart';

class Tray extends StatelessWidget {
  const Tray({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
